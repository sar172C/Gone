package gone;

public class Player_White {
	//TODO Write remaining pseudo-code
	//int[] adjacent spaces
	
	/*
	 * private Player_White(board space){
	 * 		adjacent = blank integer array
	 * }
	 */
	
	/*
	 * private Player_White(board space, number of columns){
	 * 		adjacent = getAdjacentSpaces(board space, number of columns)
	 */
	
	
	
	/*
	 * Player white should be able to:
	 * 
	 * Check if adjacent to another pieces
	 * In order to check adjacency, the size of the board is required and can be input as a parameter.
	 * Due to the legal placements of pieces, only cardinal directions need to be checked at any time.
	 */
	/*
	 * 
	 * (Integer Array) getAdjacentSpaces(number of columns) {
		boardCopy = number of columns
		output = array to hold board spaces
		
		if there is a piece at (This pieces space minus number of columns), and that space
		to array
		if there is a piece at (This pieces space plus number of columns), add that space to array
		if there is a piece at (This space +1) add space to array
		if there is a piece at (this space -1) add to array
				
	}
	 
	 * 
	 * 
	 * Return if piece is white
	 * boolean isWhite(){
	 * 	Piece is white, return true
	 * }
	 */
}
